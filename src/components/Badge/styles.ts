import { styled } from "@/stitches.config";

export const Badge = styled('div', {
  padding: '2px 12px',
  borderRadius: '12px',
  width: 'fit-content',
  height: 'fit-content',
  whiteSpace: 'nowrap',
  background: '$primary200'
})