import { styled } from "@/stitches.config";

export const Text = styled('span', {
  fontWeight: 'bold',
  fontSize: 'max(2.2rem, 2.5vw)',
  textShadow: '$shadows$textShadow100',
  color: '$primary100'
}) 